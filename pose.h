#ifndef POSE
#define POSE

#endif // POSE
#include "heads.h"

struct data
{
    unsigned int item_num = 122;
    unsigned int row_num = 1;
    int _data[item_num];
    unsigned int _index[row_num];
};

class Pose
{
    private
};
